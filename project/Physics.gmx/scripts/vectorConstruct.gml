///vectorConstruct(vector, value, normX, normY, defaultValue, maxValue, minValue)
with (argument[0]){
     value = argument[1];
     normX = argument[2];
     normY = argument[3];
     defaultValue = argument[4];
     maxValue = argument[5];
     minValue = argument[6];
}
