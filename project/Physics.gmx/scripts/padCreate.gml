///padCreate(pad, anchor, goal, sprite, color)
with (argument[0]){
     anchor = argument[1];
     goal = argument[2];
     color = argument[4];
     goal.image_blend = argument[0].color;
     sprite = argument[3];
     sprite_index = argument[3];
}

