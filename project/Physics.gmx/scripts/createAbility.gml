///createAbility(ability, player, defaultCooldown, cooldown, type, keyboard, cost)
with (argument[0]){
     player = argument[1];
     defaultCooldown = argument[2];
     cooldown = argument[3];
     type = argument[4];
     keyboard = argument[5];
     cost = argument[6];
}
