///MMItemGroupConstruct(group, selected, itemWidth, switchTime, angle, active, esc)
with (argument[0]){
     selected = argument[1];
     goal = argument[1];
     itemWidth = argument[2];
     switchTime = argument[3];
     image_angle = argument[4];
     active = argument[5];
     esc = argument[6];
}
